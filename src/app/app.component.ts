import { Component } from '@angular/core';

@Component({
  selector: 'my-app',
  template: `
   <h1>Angular 2 Login Page Demo</h1>
   <router-outlet></router-outlet>
  `
})

export class AppComponent {
  
}